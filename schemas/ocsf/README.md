# OCSF

OCSF is CERBERO's canonical normalized event model. The exact pinned OCSF version and generated/curated compatibility data are introduced with the normalization milestone, not guessed during repository bootstrap.
