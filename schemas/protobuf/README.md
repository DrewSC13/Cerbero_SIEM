# Protobuf contracts

Milestone 1 introduces the versioned `cerbero.contracts.v1` definitions for `CerberoEnvelope`, `RawEvent`, `NormalizedEvent`, `Transformation`, and `CerberoError`.

Contract evolution must follow `CERBERO — CONTRACTS v1.0` and compatibility tests must accompany changes.
