# Database migrations

All managed schema changes are versioned. Manual untracked schema changes are not part of the supported workflow.

- `postgres/`: control-plane and relational authority.
- `clickhouse/`: normalized telemetry authority.
