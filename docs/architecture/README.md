# Architecture documentation

This directory contains implementation-facing architecture notes derived from, but never silently replacing, the external v1.0 baseline.

- `source-of-truth.md`: authoritative document index and precedence rule.
- `component-ownership.md`: language/component boundaries.
- `bootstrap-decisions.md`: Milestone 0 concrete implementation choices and non-decisions.

The original baseline documents remain outside Git by repository policy.
