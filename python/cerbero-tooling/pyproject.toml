[project]
name = "cerbero-tooling"
version = "0.0.0"
description = "Development, dataset, validation, and analytical tooling for CERBERO."
requires-python = ">=3.14,<3.15"
dependencies = []
license = "Apache-2.0"

[tool.uv]
package = false
required-version = "==0.12.13"
